# Rollout Gate Status — 2026-05-21

**Verdict: DEFER**

## Live API 호출 결과

`GET /api/internal/rollout-gate-metrics` 호출 시도 → **불가**

- 이유: Vercel deployment protection (401) + `ROLLOUT_GATE_SECRET` Encrypted-only (CLI 복호화 불가)
- 확인 사항: endpoint는 라이브 (production 배포 Ready, 10분 전)
- 차선: Vercel dashboard → Project → Settings → Environment Variables에서 직접 값 확인 가능

## 7-Axis 현황 (정적 증거 기반)

| Axis | 내용 | 임계 | 현재 값 | 상태 | 출처 |
|------|------|------|---------|------|------|
| 1 | External Adoption (distinct project_hash) | ≥ 15 | **4** | INSUFFICIENT | `verification-report.md §8.3` (Redis HLEN=4, 2026-05-21) |
| 2 | Setup Time/Fail Rate | median ≤ 10m, fail ≤ 20%, n ≥ 10 | n < 10 추정 | INSUFFICIENT | Axis 1과 n 종속 |
| 3 | Terminal Bottleneck (survey) | n ≥ 20, terminal ≥ 50% | n < 20 추정 | INSUFFICIENT | 동상 |
| 4 | Fixture Parity TS≡Python | 42/42 vitest | 188/188 PASS | **PASS** (CI_ONLY) | `.harness/log.md`, branch `parity` required |
| 5 | Client Network Security | 42/42 vitest | PASS | **PASS** (CI_ONLY) | branch `security` required |
| 6 | Server Persistence Whitelist | 정확히 9 keys | PASS | **PASS** (CI_ONLY) | branch `gate-pass` required |
| 7 | Telemetry Privacy Schema | strict schema | PASS | **PASS** (CI_ONLY) | 동상 |

**overallGatePass: false (예상)**

## Data Freshness 검증

- `axis1.value = 4` — verification-report(2026-05-21 commit `db4c974`) 기준과 동일. 오늘 recruitment 활동 없음
- `axis2 started/completed` — 텔레메트리 route `/api/telemetry/auto-detect` 배선 완료, 수집 중이나 n < 10 확실
- Redis snapshot stale 가능성: Upstash production 확인 불가(token encrypted). HLEN=4는 마지막 확인 기준

## 차단 항목

1. **Axis 1 < 15** — 외부 사용자 11명 추가 필요. recruitment 미실행 → Axes 2/3 n도 동시에 막힘
2. **Kill-switch 미배선** — `NEXT_PUBLIC_AUTO_DETECT_DEFAULT` env var가 codex-r3/five-axis-v2.md에 정의됐으나 `JoinBurnIndexForm.tsx:62-66`에 미구현. 현재 flip = 코드 배포 필요, env 전환만으로 OFF 불가 → 사고 시 즉시 rollback 수단 없음 (Vercel Instant Rollback만 가능)

## 다음 측정 트리거 (재판단 시점)

| # | 트리거 | 예상 시기 |
|---|--------|-----------|
| 1 | `Axis 1 ≥ 15` OR recruitment 캠페인 완료 | axis1-recruitment-strategy.md 실행 착수 후 1-2주 |
| 2 | 2026-05-28 (1주 후) — 정기 check | 자동 |
| 3 | kill-switch 배선 완료 — flip 선행 조건 | 별도 PR 필요 |

Axis 1 ≥ 10 시점은 **flip 재평가 아님** — Axes 2/3 telemetry sanity check 용도로만 사용 (Axis 3 hard gate = n ≥ 20)

## 준비 작업 (다음 사이클, 이번 체크 범위 외)

### P1: Kill-switch 배선 (flip 선행 필수)

파일: `web/components/forms/JoinBurnIndexForm.tsx:62-66`

```diff
-const autoDetect =
-  params.get("auto-detect") === "1" &&
-  typeof window !== "undefined" &&
-  "showDirectoryPicker" in window;
+const autoDetect =
+  (params.get("auto-detect") === "1" ||
+   process.env.NEXT_PUBLIC_AUTO_DETECT_DEFAULT === "true") &&
+  typeof window !== "undefined" &&
+  "showDirectoryPicker" in window;
```

추가 필요: `web/.env.example`에 `NEXT_PUBLIC_AUTO_DETECT_DEFAULT=false` 항목 추가

### P2: Axis 1 Recruitment 실행

문서: `tasks/production-rollout-gate/axis1-recruitment-strategy.md`

목표: 외부 사용자 11명+ 확보 → Axis 1 ≥ 15 도달 후 Axes 2/3 telemetry 자동 축적

---

*작성: 2026-05-21 | 기준: verification-report.md §8.3 + 정적 코드 분석*
*Live API 불가 사유: Vercel deployment protection + secret encrypted-only*

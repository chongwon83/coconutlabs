# A.12 Pre-merge UX Hardening — Resume Handoff (2026-05-25)

이 문서는 autocompact thrashing으로 세션 정리 후, 새 세션에서 A.12를 이어서 진행하기 위한 단일 진입점.

---

## 새 세션 첫 메시지 (verbatim 사용 가능)

```
이전 세션에서 autocompact thrashing으로 정리하고 재진입.
docs/handoff/2026-05-25-a12-resume.md를 먼저 읽고, 거기 명시된 "다음 1 action"부터 이어줘.
plan: ~/.claude/plans/nested-singing-whale.md (A.12 섹션 잠금)
권한: 모두 부여. 자율 진행.
```

---

## 현재 상태 (lock)

**Branch**: `coconut-burn-live-track-a` (PR #21 미머지)
**Plan**: `~/.claude/plans/nested-singing-whale.md` A.12 섹션 잠금 (D1/D2/D3 owner lock)
**Memory**: 6건 자동 로드 (재기록 불필요)

### A.12 진행 상태

| # | Edit | 상태 | 파일 / 앵커 |
|---|------|------|-------------|
| 1 | useState/useRef 추가 | ✅ | `components/forms/JoinBurnIndexForm.tsx` L105-107 |
| 2 | useEffect scrollIntoView+focus | ✅ | L113-120 |
| 3 | FSA early-return guard | ✅ | L261 |
| 4 | Manual early-return guard | ✅ | L360 |
| 5 | FSA success card (stacked with PostUploadSurvey) | ✅ | L405-422, L541-578 |
| 6 | Manual success card | ✅ | L660-694 |
| 7 | Manual POST 2xx gate + catch | ✅ | L383-396 |
| 8 | globals.css success-card classes | ✅ | `app/globals.css` L2243-2288 (8 occurrences) |
| **9** | **E2E spec 신규** | **⏳ 다음 1 action** | `e2e/upload-success-card.spec.ts` 신규 ~80 LOC |

---

## 다음 1 Action: Edit #9 작성

### 파일: `e2e/upload-success-card.spec.ts` (신규)

### 참조 패턴 (chunked read 권장)
- **Route stub pattern**: `e2e/burn-index-sort.spec.ts` L80-92 (GET passthrough via `route.continue()`, POST fulfill)
- **FSA picker fixture**: `e2e/burn-import-fsa-picker.spec.ts` L1-80 (`addInitScript` for `showDirectoryPicker` stub) — Branch 1만 필요
- **Envelope schema**: `lib/validateSummary.ts` L31 (row 9 fields), L37 VERIFICATION_KEYS, L88-91 whitelists, L142 projectHash `/^[0-9a-f]{12}$/`

### 4 branches

| # | 시나리오 | 핵심 검증 |
|---|---------|----------|
| 1 | Manual success | POST→200, 모달 진입→`#jbi-handle`+`#jbi-paste`→Validate→Add→success card visible (`role=status`, "리더보드에 추가되었어요", `@{handle}`, "리더보드 보기") |
| 2 | Failure 500 | POST→500, CTA 그대로 visible, success card NOT in DOM |
| 3 | Double-submit + aria-busy | pending 중 `aria-busy="true"`, 성공 후 button disabled or replaced |
| 4 | Reduced motion | `emulateMedia({reducedMotion:'reduce'})` 후에도 scrollIntoView 발생 + focus 이동 |

### 진입 anchor
- 모달 트리거: `[data-testid="hero-cta-primary"]` (Hero.tsx L129-130, text "Join Burn Index")
- Manual form: `#jbi-handle` / `#jbi-paste` / "Validate & preview" submit / "Add to Burn Index" button (L686)
- Success card: `.upload-success-card` / `.upload-success-card__title` / `.upload-success-card__cta`

### 검증 (Edit #9 후 즉시 실행)
```bash
rg -n "upload-success-card|showSuccess" e2e/upload-success-card.spec.ts components/forms/JoinBurnIndexForm.tsx app/globals.css
```

---

## A.12 잔여 Verification Gates (Edit #9 완료 후 순차)

1. `npx tsc --noEmit` → 0 errors
2. `npx vitest run` → green (기존 회귀 0)
3. `npx playwright test e2e/upload-success-card.spec.ts e2e/burn-index-sort.spec.ts e2e/burn-index-filter.spec.ts e2e/onboarding-30s.spec.ts` → green
4. `npx @google/design.md lint DESIGN.md` → error 0
5. Chrome MCP self-verify 4 branches (Preview or local dev)
6. `/codex review` on full diff (default gpt-5.5 only, **NOT** `-m gpt-5-codex` — 400 hang)
7. Commit (**NO Co-Authored-By**, solo project): `feat(burn): add inline upload success card + a11y hardening`
8. push → Preview redeploy → owner manual smoke
9. A.11 Step 4: `gh api -X PUT /repos/chongwon83/coconutlabs/pulls/21/merge --field merge_method=squash`
10. A.11 Step 5: GH Actions `workflow_dispatch` visual rebaseline (Linux only, 3 PNGs)
11. A.11 Step 6: `docs/workflow-state.md` 갱신 + Track B 진입 안내

---

## 절대 invariants (재진입 후에도 위반 금지)

- `lib/validateSummary.ts:52` VerifLevel 4-union 무수정
- `lib/validateSummary.ts:48` TOOLS 2-union 무수정
- `__tests__/burn-display-labels.test.ts:14` green 유지
- DESIGN.md lint error 0
- macOS PNG commit 금지 (Linux CI workflow_dispatch only)
- E2E selector `.lb-row .lb-handle` + `[role="columnheader"]` 보존
- `JoinBurnIndexForm` props는 `{ onSuccess, onImport }` 2개뿐 — **onClose 없음**. "리더보드 보기" 버튼은 `window.location.hash = "#burn"` 단독
- `handleFsaUpload`/`handleConfirm` 모두 `!res.ok`에서 **early return** (throw 아님). `setShowSuccess(true)`는 POST 2xx 분기 안에서만
- Korean copy: "리더보드에 추가되었어요" + "리더보드 보기"
- aria-live="polite" (assertive 아님)
- scrollIntoView `block:'nearest'` + `prefers-reduced-motion` → `behavior:'instant'`
- Solo project: PR review request 없음, Co-Authored-By 라인 없음

---

## Out of A.12 scope (재진입 후에도 손대지 말 것)

- `components/BurnIndexSection.tsx` (Track B)
- `DESIGN.md` token 추가 (Track B B.6)
- PostUploadSurvey를 manual path 확장 (D1 lock)
- `position: sticky` (codex #2 reject)
- `.lb-row--pulse` (codex #5 reject)
- Hero stat bar 와이어링 (Track B B.3)
- CTA color/size 재디자인 (Track B B.1)
- SWR polling (Track B B.2)

---

## 작성자 메모

이 핸드오프 자체는 ≤ 200 lines 유지. 새 세션이 이 파일 1개만 읽으면 코드/플랜 본문 재로딩 없이 Edit #9부터 즉시 이어갈 수 있도록 설계.
